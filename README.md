# proofsnap-verify
Standalone web app for verifying ProofSnap forensic evidence packages. Validates SHA-256 hashes, blockchain timestamps (OpenTimestamps), eIDAS qualified seals, chain of custody, and digital signatures from ProofSnap ZIP bundles. Client-side only — no uploads, no server processing.
